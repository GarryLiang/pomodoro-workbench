# 更新日志（Changelog）

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 风格，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### 计划中
- 今日目标番茄数与专注进度环
- 最小化到系统托盘与系统通知
- 英文界面（i18n）
- 详细路线图见 [ROADMAP.md](ROADMAP.md)

## [1.3.1] - 2026-09-11

修复 v1.3.0 发布构建中的时钟取整缺陷（补丁版本，功能与 v1.3.0 相同）。

### 修复
- **剩余时间偶发"回退 1 秒"**：`pause()` / `refresh()` 在极少数情况下会把刚扣减的
  1 秒显示回去（`ceil((t+N) - t)` 的浮点舍入可能得到 `N+1`）。
  现在时钟计算减去 1 微秒容限（`CLOCK_EPSILON`），并新增 300 次 tick+pause
  回归测试防止复发。
- 修正测试中两处对时间敏感的断言（暂停基准、连续 refresh 允许 1 秒真实流逝），
  使持续集成在 Windows 高负载下稳定通过。

## [1.3.0] - 2026-09-11

计时精度、跨平台与稳定性版本（代码审查后的一轮质量修复）。

### 修复
- **运行中修改时长导致进度条出现负值、倒计时与设置不一致**：引擎现在为每个阶段
  快照总时长，进行中的阶段保持原节奏，新时长从下一阶段生效（设置面板会给出提示）。
- **计时会随界面卡顿变慢**：改为按单调时钟计算剩余时间，界面每 250ms 只做刷新；
  `refresh()` 幂等，重复调用不会重复扣减时间。
- `make_card(padding=…)` 的 `padding` 参数此前只被存为属性、从未生效，现已移除该误导参数。

### 改进
- **意外异常写入日志**：新增 `app/logger.py`，界面刷新/主循环的非预期异常记录到
  `~/.pomodoro_workbench/app.log`（自动轮转），便于用户反馈问题时排查；
  TclError（窗口销毁竞态）仍按预期静默忽略。
- **主窗口位置与尺寸记忆**：关闭时保存 `win_geometry`，下次启动恢复。
- **跨平台字体自适应**：按 Windows / macOS / Linux 探测可用中文字体
  （微软雅黑 / 苹方 / Noto Sans CJK 等），找不到时回退 Tk 默认字体。
- **悬浮窗增强**：新增 📌 置顶开关（可关闭置顶，设置持久化）；隐藏时不再刷新界面；
  支持多显示器负坐标（可放到主屏左侧/上方的副屏），越界坐标自动回退默认位置。
- **首次使用引导**：首启显示一条三步上手提示，点「知道了」后不再出现。
- **数据库结构版本**：`settings.schema_version` + 迁移通道（`MIGRATIONS`），
  为后续表结构变更预留升级路径。
- 清理死代码：`ui/theme.py` 的 `hex_to_rgb`/`mix_alpha`、`app/alarm.py` 的
  `all_alarm_fingerprints()`、`app/export.py` 未使用的 `kind_label` 参数、
  `app/stats.py` 中恒无意义的条件判断。
- 核心测试 93 → **108 项**；GUI 冒烟新增端到端用例（阶段结束 → 记录会话 →
  累计待办番茄数）与窗口位置记忆、置顶开关、首启引导检查。

## [1.2.0] - 2026-09-10

迷你悬浮窗版本。

### 新增
- 🪟 **迷你悬浮窗**：主窗口最小化后自动弹出（可在侧边栏关闭该行为），
  也可手动勾选开关；约 240×160 像素的无边框置顶小窗，显示当前阶段、
  剩余时间与进度条，内置「开始 / 暂停」「跳过」「回主界面」三个快捷按钮；
  支持鼠标拖动与位置记忆，配色跟随主题并随主题切换自动重建
  （`ui/float_window.py`）。
- 新增 3 项悬浮窗设置：显示开关、最小化自动显示、窗口位置记忆。

### 文档
- README 新增悬浮窗功能说明与使用步骤，截图更新为 10 张。

## [未发布]

### 计划中
- 今日目标番茄数与专注进度环
- 最小化到系统托盘与系统通知
- 英文界面（i18n）
- 详细路线图见 [ROADMAP.md](ROADMAP.md)

## [1.1.0] - 2026-09-09

主题与提醒自定义版本。

### 新增
- 🎨 **界面主题系统**：4 套配色一键切换（默认浅色 / **深色模式** / 护眼绿 /
  樱花粉），侧边栏直接切换、重启后保持；深色模式下卡片、热力图、趋势图、
  提醒弹窗全部适配（`app/themes.py` + `ui/theme.py`）。
- ⏰ **闹钟提醒自定义**：弹窗提醒 / 响铃提醒可分别开关；**随时试听**三种阶段
  铃声；自动停铃时长可调（5~120 秒）；支持**导入自定义 WAV 铃声**替代内置音效，
  文件失效时自动回退内置铃声。
- 💾 **数据导出与一键备份**：待办 / 习惯打卡 / 专注记录 / 全部阶段记录
  一键导出 CSV（UTF-8-BOM，Excel 直接打开不乱码），数据库一键备份与还原
  （统计页 → 数据导出与备份，来自社区 PR #1）。

### 改进
- 页面重建时自动注销番茄钟事件监听，避免重复回调。
- ttk 样式表全面参数化到主题调色板，硬编码颜色清零。
- 新增主题调色板（13 项）与提醒设置（9 项）自动化测试，核心测试共 **90 项**。

## [1.0.0] - 2026-09-09

首个正式版本：零第三方依赖的三合一桌面效率工具。

### 新增
- 🍅 **番茄专注**：专注/短休息/长休息计时，时长与长休息间隔可调；
  开始/暂停/继续/重置/跳过；可自动进入下一阶段。
- ⏰ **三阶段专属闹钟提醒**：每个阶段结束自动弹出置顶提醒窗，并播放
  现场合成（WAV）的专属铃声（专注=凯旋琶音 / 短休息=柔和双音 /
  长休息=冲刺号角）；可整体关闭提醒。
- ☑ **待办清单**：增删改查、优先级（高/中/低）、截止日期与逾期标红、
  按状态/优先级筛选、完成率统计；与番茄钟联动自动累计"番茄数"。
- 🔥 **习惯打卡**：每日打卡/取消、总天数/当前连续/最长连续/本月完成率、
  15 周 GitHub 风格打卡热力图。
- 📊 **数据统计**：今日/累计番茄与专注分钟、近 7 天专注趋势柱状图、
  待办与习惯完成率进度条。
- 🗄 **本地持久化**：SQLite 单文件存储（`~/.pomodoro_workbench/data.db`）。

### 工程与体验
- 纯 Python 标准库实现，零第三方依赖（应用本体）。
- 一键启动：`start.bat`（Windows，缺 Python 可自动安装）/
  `start.sh`（macOS/Linux）；`build_exe.bat` 可打包独立 exe。
- 核心逻辑（`app/` 包）与界面分离，可被第三方程序 `import` 引用。
- 62 项核心逻辑断言 + GUI 冒烟测试全部通过。
- 完整中文文档、真实运行截图、MIT License。
